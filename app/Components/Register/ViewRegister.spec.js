'use strict';

describe('myApp.Register module', function() {

    beforeEach(module('myApp.Register'));

    describe('ViewRegisterCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewRegisterCtrl = $controller('ViewRegisterCtrl');
            expect(viewRegisterCtrl).toBeDefined();
        }));

    });
});