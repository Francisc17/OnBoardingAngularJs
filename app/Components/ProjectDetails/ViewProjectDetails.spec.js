'use strict';

describe('myApp.ProjectDetails module', function() {

    beforeEach(module('myApp.ProjectDetails'));

    describe('ViewProjectDetailsCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewProjectDetailsCtrl = $controller('ViewProjectDetailsCtrl');
            expect(viewProjectDetailsCtrl).toBeDefined();
        }));
    });
});