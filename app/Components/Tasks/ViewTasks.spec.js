'use strict';

describe('myApp.Tasks module', function() {

    beforeEach(module('myApp.Tasks'));

    describe('ViewTasksCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewTasksCtrl = $controller('ViewTasksCtrl');
            expect(viewTasksCtrl).toBeDefined();
        }));

    });
});