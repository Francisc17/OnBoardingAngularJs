'use strict';

describe('myApp.AddProject module', function() {

    beforeEach(module('myApp.AddProject'));

    describe('ViewAddProjectCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewAddProjectCtrl = $controller('ViewAddProjectCtrl');
            expect(viewAddProjectCtrl).toBeDefined();
        }));
    });
});