'use strict';

describe('myApp.TaskDetails module', function() {

    beforeEach(module('myApp.TaskDetails'));

    describe('ViewTaskDetailsCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewTaskDetailsCtrl = $controller('ViewTaskDetailsCtrl');
            expect(viewTaskDetailsCtrl).toBeDefined();
        }));
    });
});