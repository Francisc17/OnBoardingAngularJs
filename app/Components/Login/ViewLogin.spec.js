'use strict';

describe('myApp.Login module', function() {

    beforeEach(module('myApp.Login'));

    describe('ViewLoginCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewLoginCtrl = $controller('ViewLoginCtrl');
            expect(viewLoginCtrl).toBeDefined();
        }));
    });
});