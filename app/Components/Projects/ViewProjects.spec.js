'use strict';

describe('myApp.Projects module', function() {

    beforeEach(module('myApp.Projects'));

    describe('ViewProjectsCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewProjectsCtrl = $controller('ViewProjectsCtrl');
            expect(viewProjectsCtrl).toBeDefined();
        }));
    });
});